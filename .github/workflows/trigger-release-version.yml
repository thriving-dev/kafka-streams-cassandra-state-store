on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: true

jobs:
  release-version:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Version: $VERSION"
        env:
          VERSION: ${{ inputs.version }}
